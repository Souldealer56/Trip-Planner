# GitHub Integration Rules

- **Branch Management**: Feature work should use `main` or feature branches created via `/gsd-pr-branch`.
- **Ignore Rules**: Verify that `.env` and `venv/` are never included in `git add`.
- **GSD Workflow**: GSD auto-tracks plan progress and atomic commits in Git history.
