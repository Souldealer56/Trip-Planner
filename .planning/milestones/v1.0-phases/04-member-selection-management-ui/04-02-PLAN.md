---
phase: 04-member-selection-management-ui
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - web/src/views/TripDetails.jsx
autonomous: true
requirements:
  - MEMBER-02
user_setup: []

must_haves:
  truths:
    - "Vite compiles successfully and bundles updated join forms"
    - "Submitting join form executes hook mutation and triggers roster reload"
    - "Newly registered user is automatically logged in and closes the modal"
  artifacts:
    - "web/src/views/TripDetails.jsx"
  key_links:
    - "web/src/views/TripDetails.jsx imports useAddParticipant from web/src/hooks/useAddParticipant.js"
---

<objective>
Implement the new participant registration form inside the overlay modal, enabling anonymous visitors to join the trip, auto-login, and immediately sync with the database.

Purpose: Completes the collaborative onboarding flow for web users.
Output: Integrated Join Trip form inside modal, linked to Supabase mutation hooks.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-member-selection-management-ui/04-CONTEXT.md
@.planning/phases/04-member-selection-management-ui/04-RESEARCH.md
@.planning/phases/04-member-selection-management-ui/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Join Trip form inside profile modal in TripDetails</name>
  <files>web/src/views/TripDetails.jsx</files>
  <read_first>web/src/views/TripDetails.jsx, web/src/hooks/useAddParticipant.js, .planning/phases/04-member-selection-management-ui/04-CONTEXT.md</read_first>
  <action>
    Modify `web/src/views/TripDetails.jsx` per D-02 to load and trigger `useAddParticipant` hook.
    Inside the profile selection modal markup:
    1. Add a button "I'm not on this list (Join Trip)" which toggles showing the registration form panel.
    2. Build a form displaying:
       - First Name input (required).
       - Telegram Username input (optional, strips leading `@`).
       - Submit button ("Join & Sign In").
       - Back button (toggles back to profile selection list).
    3. Implement a submit handler `handleJoinSubmit(e)` that:
       - Calls `addParticipant(id, username, firstName)`.
       - On success, triggers `onLogin(user)` with the returned user record (auto-login).
       - Triggers `refreshRoster()` to reload the trip participants.
       - Resets form fields.
    If the roster is empty initially, show the form panel by default.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `TripDetails.jsx` imports `useAddParticipant` hook
    - Form fields include controlled firstName and username state variables
    - Form submission calls hook, login, and refreshes roster
  </acceptance_criteria>
  <done>Join form is built into the selector modal, triggering user creation and automatic login session.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` completes successfully with exit code 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Participant registration forms and session contexts verified
</success_criteria>

<output>
After completion, create `.planning/phases/04-member-selection-management-ui/04-02-SUMMARY.md`
</output>
