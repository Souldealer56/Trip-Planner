---
phase: 04-member-selection-management-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/src/styles/global.css
  - web/src/views/TripDetails.jsx
autonomous: true
requirements:
  - MEMBER-01
user_setup: []

must_haves:
  truths:
    - "Vite compiles successfully and bundles App.jsx session state updates"
    - "global.css defines overlay modal backdrop and content parameters"
    - "TripDetails renders blocking modal selector if activeUser is null"
  artifacts:
    - "web/src/App.jsx"
  key_links:
    - "web/src/App.jsx propagates activeUser state and handlers to views"
---

<objective>
Lift active user state to App.jsx, define layout styling parameters for modal backdrops and lists, and build the profile selection overlay modal inside TripDetails.

Purpose: Establishes app-level session simulation, allowing users to choose an existing profile.
Output: App state lifecycle configured, overlay designs loaded, and blocking selector modal created.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-member-selection-management-ui/04-CONTEXT.md
@.planning/phases/04-member-selection-management-ui/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift activeUser state in App.jsx and add overlay layout styles in global.css</name>
  <files>web/src/App.jsx, web/src/styles/global.css</files>
  <read_first>web/src/App.jsx, web/src/styles/global.css</read_first>
  <action>
    Modify `web/src/App.jsx` per D-03 to manage `activeUser` state initialized from `localStorage` key `trip_planner_active_user`.
    Define and pass handlers `onLogin(user)` (saves to localStorage and sets state) and `onLogout()` (removes from localStorage and resets state) to rendering components.
    Modify `web/src/styles/global.css` to add modal overlay layout system (`.modal-overlay`, `.modal-content`), member list grid options, and session header widgets.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/src/App.jsx` maintains activeUser React state and propagates handlers
    - `web/src/styles/global.css` contains modal layout styles (.modal-overlay, etc.)
  </acceptance_criteria>
  <done>App-level session context is implemented and modal styling properties are loaded.</done>
</task>

<task type="auto">
  <name>Task 2: Build blocking profile selection modal on TripDetails page</name>
  <files>web/src/views/TripDetails.jsx</files>
  <read_first>web/src/views/TripDetails.jsx, .planning/phases/04-member-selection-management-ui/04-CONTEXT.md</read_first>
  <action>
    Modify `web/src/views/TripDetails.jsx` per D-01 and D-03.
    Accept `activeUser`, `onLogin`, and `onLogout` props.
    In the header section: if `activeUser` is set, render their profile name (e.g. "Signed in as: Alex") and a "Logout" action.
    If `activeUser` is not set:
    1. Display a blocking overlay card `.modal-overlay`.
    2. Render title "Select Your Profile" and short explanation.
    3. Render buttons for each participant in `roster` to let user pick their profile. Clicking a button triggers `onLogin(member.users)`.
    Exclude the modal if data is still loading or in error state.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `TripDetails.jsx` receives and handles session props
    - Renders select profile modal containing roster members if activeUser is null
    - Renders logged in header panel with logout trigger
  </acceptance_criteria>
  <done>TripDetails view is modified to block anonymous access via profile selection modal.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` completes successfully with exit code 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-member-selection-management-ui/04-01-SUMMARY.md`
</output>
