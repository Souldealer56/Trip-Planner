---
phase: 02-database-data-access-layer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/services/trips.js
  - web/src/services/rsvps.js
  - web/src/hooks/useTrips.js
  - web/src/hooks/useTripDetails.js
  - web/src/hooks/useRsvpRoster.js
autonomous: true
requirements:
  - TRIPS-01
  - TRIPS-02
  - DETAIL-01
  - DETAIL-02
user_setup: []

must_haves:
  truths:
    - "Vite compilation bundles the new trips and rsvps service modules successfully"
    - "useTrips hook fetches and lists all active trips in order"
    - "useTripDetails hook retrieves specific trip metadata by id"
    - "useRsvpRoster hook loads rsvp statuses and related user objects in a single join query"
  artifacts:
    - "web/src/services/trips.js"
    - "web/src/services/rsvps.js"
    - "web/src/hooks/useTrips.js"
    - "web/src/hooks/useTripDetails.js"
    - "web/src/hooks/useRsvpRoster.js"
  key_links:
    - "web/src/hooks/useTrips.js imports fetchTrips from web/src/services/trips.js"
    - "web/src/hooks/useRsvpRoster.js imports fetchRsvpRoster from web/src/services/rsvps.js"
---

<objective>
Implement the core fetching services and custom hooks for retrieving trips, specific trip details, and RSVP participant rosters.

Purpose: Establishes a read-only data access layer for all trip and participant views.
Output: Service modules and React hooks for querying trips, details, and RSVPs.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-data-access-layer/02-CONTEXT.md
@.planning/phases/02-database-data-access-layer/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database fetching services for trips and RSVPs</name>
  <files>web/src/services/trips.js, web/src/services/rsvps.js</files>
  <read_first>web/src/services/supabase.js, .planning/phases/02-database-data-access-layer/02-CONTEXT.md</read_first>
  <action>
    Create `web/src/services/trips.js`. Import the initialized `supabase` client.
    Write and export `fetchTrips()` which queries the `trips` table selecting `id, title, destination, start_date, end_date, base_currency` ordered by `start_date` ascending.
    Write and export `fetchTripById(id)` which selects all fields from the `trips` table matching the specific trip ID.
    Create `web/src/services/rsvps.js`. Write and export `fetchRsvpRoster(tripId)` which queries `rsvps` table selecting `status, user_id, users(*)` where `trip_id` matches, performing a native join on the `users` table per D-01.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/src/services/trips.js` exists and exports fetchTrips and fetchTripById
    - `web/src/services/rsvps.js` exists and exports fetchRsvpRoster
    - `fetchRsvpRoster` queries users relationship using users(*) select notation
  </acceptance_criteria>
  <done>Fetching services for trips and RSVPs are written and bundle cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement custom React hooks useTrips, useTripDetails, and useRsvpRoster</name>
  <files>web/src/hooks/useTrips.js, web/src/hooks/useTripDetails.js, web/src/hooks/useRsvpRoster.js</files>
  <read_first>web/src/services/trips.js, web/src/services/rsvps.js, .planning/phases/02-database-data-access-layer/02-CONTEXT.md</read_first>
  <action>
    Create custom hooks in `web/src/hooks/`:
    - `useTrips.js`: Runs `fetchTrips()`, manages state for `data`, `loading`, and `error`. Returns `{ data, loading, error, refresh }` where refresh is a callback triggering reload.
    - `useTripDetails.js`: Takes a `tripId` parameter. Runs `fetchTripById(tripId)`, manages load/error state, and returns `{ data, loading, error, refresh }`.
    - `useRsvpRoster.js`: Takes `tripId`. Runs `fetchRsvpRoster(tripId)`, manages state, and returns `{ data, loading, error, refresh }` per D-01 and D-03.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/src/hooks/useTrips.js` exists and exports useTrips hook
    - `web/src/hooks/useTripDetails.js` exists and exports useTripDetails hook
    - `web/src/hooks/useRsvpRoster.js` exists and exports useRsvpRoster hook
    - All hooks manage and return data, loading, error, and refresh fields
  </acceptance_criteria>
  <done>React hooks useTrips, useTripDetails, and useRsvpRoster are created and return loading, error, and refresh triggers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` completes successfully with exit code 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-data-access-layer/02-01-SUMMARY.md`
</output>
