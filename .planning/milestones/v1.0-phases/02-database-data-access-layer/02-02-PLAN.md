---
phase: 02-database-data-access-layer
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - web/src/services/users.js
  - web/src/services/rsvps.js
  - web/src/hooks/useAddParticipant.js
  - web/verify-services.cjs
autonomous: true
requirements:
  - MEMBER-01
  - MEMBER-02
user_setup: []

must_haves:
  truths:
    - "Vite compilation bundles the user registration service and hook successfully"
    - "createUser service inserts user details and generates unique negative telegram_id"
    - "verify-services.cjs successfully executes select and insert/delete verification cycles"
  artifacts:
    - "web/src/services/users.js"
    - "web/src/hooks/useAddParticipant.js"
    - "web/verify-services.cjs"
  key_links:
    - "web/src/hooks/useAddParticipant.js imports createUser from web/src/services/users.js"
    - "web/src/hooks/useAddParticipant.js imports createRsvp from web/src/services/rsvps.js"
---

<objective>
Implement database mutation services and hooks for registering new participants and linking them to trip RSVPs, and establish a database integration verify script.

Purpose: Enables participants to join trips from the web app, and verifies database read/write access.
Output: Service functions, custom hook, and database integration verification script.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-data-access-layer/02-CONTEXT.md
@.planning/phases/02-database-data-access-layer/02-RESEARCH.md
@.planning/phases/02-database-data-access-layer/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement user insertion and RSVP mutation services</name>
  <files>web/src/services/users.js, web/src/services/rsvps.js</files>
  <read_first>web/src/services/supabase.js, .planning/phases/02-database-data-access-layer/02-CONTEXT.md</read_first>
  <action>
    Create `web/src/services/users.js`. Import the `supabase` client.
    Write and export `createUser(username, firstName)` which generates a unique negative `telegram_id` using `-1000000 - Math.floor(Math.random() * 1000000)` per D-02.
    It should insert the new user record (`telegram_id`, `username` or null, `first_name`) into the `users` table and return the inserted record details.
    Modify `web/src/services/rsvps.js` to add and export `createRsvp(tripId, userId, status)`.
    It inserts a new record (`trip_id`, `user_id`, `status` defaulting to 'Committed') into the `rsvps` table and returns the inserted RSVP details.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/src/services/users.js` exists and exports `createUser`
    - `web/src/services/rsvps.js` exports `createRsvp`
    - `createUser` generates negative `telegram_id` values
  </acceptance_criteria>
  <done>User creation and RSVP joining service functions are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement custom React hook useAddParticipant</name>
  <files>web/src/hooks/useAddParticipant.js</files>
  <read_first>web/src/services/users.js, web/src/services/rsvps.js, .planning/phases/02-database-data-access-layer/02-CONTEXT.md</read_first>
  <action>
    Create custom hook `web/src/hooks/useAddParticipant.js` per D-01.
    Write and export `useAddParticipant()`. It handles loading and error states for participant addition.
    It exposes a function `addParticipant(tripId, username, firstName)` that:
    1. Calls `createUser(username, firstName)`.
    2. Uses the returned user's `id` to call `createRsvp(tripId, userId, 'Committed')`.
    3. Returns the joined participant/user object.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/src/hooks/useAddParticipant.js` exists and exports the custom hook
    - The hook calls createUser and createRsvp sequentially and manages load/error states
  </acceptance_criteria>
  <done>useAddParticipant custom hook is implemented to encapsulate registration workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Implement and execute database integration verification script</name>
  <files>web/verify-services.cjs</files>
  <read_first>web/src/services/trips.js, web/src/services/rsvps.js, web/src/services/users.js, .env</read_first>
  <action>
    Create `web/verify-services.cjs` to test database operations.
    Write logic to:
    1. Parse credentials from `../.env`.
    2. Import `@supabase/supabase-js`.
    3. Perform test actions:
       - Query and check if trips can be fetched.
       - Insert a test user with a specific, unique negative telegram_id (e.g. `-9999999`).
       - If there is at least one trip in the database, insert a test committed RSVP connecting that test user to the trip.
       - Query the rsvps table to verify the test user joined.
       - Clean up: delete the test user from the `users` table by their negative telegram_id (which automatically cascade deletes the test RSVP).
    If all checks succeed, log success and exit 0. If any query fails, log the error and exit 1.
  </action>
  <verify>
    Run: `node web/verify-services.cjs`
  </verify>
  <acceptance_criteria>
    - `web/verify-services.cjs` exists
    - Script compiles, performs connection checks, and exits successfully
  </acceptance_criteria>
  <done>verify-services.cjs is implemented and database integration testing is verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` completes successfully with exit code 0
- [ ] `node web/verify-services.cjs` runs and exits with code 0 (logs query/mutation checks)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Database queries and participant mutations are verified successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-data-access-layer/02-02-SUMMARY.md`
</output>
