---
phase: 05-full-system-integration-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/vite.config.js
  - web/cleanup-e2e.cjs
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - TRIPS-01
  - TRIPS-02
  - DETAIL-01
  - DETAIL-02
  - MEMBER-01
  - MEMBER-02
user_setup: []

must_haves:
  truths:
    - "Vite compiles successfully and bundles dev port configurations"
    - "Local dev server boots and serves the application at strict port 5173"
    - "Browser subagent navigates to dev server, triggers modal selection, submits join form, and verifies auto-login session"
    - "cleanup-e2e.cjs successfully removes test participant records from Supabase tables"
  artifacts:
    - "web/cleanup-e2e.cjs"
---

<objective>
Execute the full system verification checklist. Configure strict ports, implement E2E database cleanup routines, and orchestrate browser subagent automation checks to confirm all trip lists, routing navigation, profile selection modals, user registration forms, and database integrations are completely operational.

Purpose: Confirms full milestone completion and proves correctness of visual and storage states.
Output: Server configurations, test cleanup script, and browser verification artifacts.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-system-integration-verification/05-CONTEXT.md
@.planning/phases/05-full-system-integration-verification/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set strict server ports in vite.config.js and create cleanup-e2e.cjs</name>
  <files>web/vite.config.js, web/cleanup-e2e.cjs</files>
  <read_first>web/vite.config.js, .planning/phases/05-full-system-integration-verification/05-CONTEXT.md</read_first>
  <action>
    Modify `web/vite.config.js` to ensure the server enforces strict port bounds: add `server: { port: 5173, strictPort: true }`.
    Create `web/cleanup-e2e.cjs`. Write code to load credentials from `../.env`, initialize the Supabase client, query the `users` table to locate any test user whose first name is exactly "Subagent E2E User", and delete them.
    The script should log results and exit with 0 on success.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/vite.config.js` configures port 5173 and strictPort true
    - `web/cleanup-e2e.cjs` exists and compiles
  </acceptance_criteria>
  <done>Strict ports are configured and the database E2E cleanup script is written.</done>
</task>

<task type="auto">
  <name>Task 2: Boot local dev server and execute browser subagent verification tests</name>
  <files>web/vite.config.js</files>
  <read_first>web/src/views/TripDetails.jsx, .planning/phases/05-full-system-integration-verification/05-CONTEXT.md</read_first>
  <action>
    Start the Vite development server in the background using:
    `npm run dev --prefix web`
    Wait 2 seconds for server boot.
    Launch the `browser_subagent` to execute E2E visual verification:
    - Load `http://localhost:5173/`
    - Verify page title is 'Trip Planner'
    - Click on the first trip in the list card grid
    - Confirm routing navigation loads details and displays the profile selection overlay modal
    - Click 'I'm Not on This List (Join Trip)'
    - Input First Name: 'Subagent E2E User', Telegram Username: 'subagent_test'
    - Click 'Join & Sign In'
    - Verify modal disappears, header displays 'Signed in as: Subagent E2E User', and roster contains the new user with a Committed badge.
    Wait for browser subagent return.
    Once subagent report is received, kill the background dev server task.
  </action>
  <verify>
    Check subagent report and confirm it matches expected states.
  </verify>
  <acceptance_criteria>
    - Dev server starts and serves application
    - Browser subagent executes routing navigation, form submission, and profile updates successfully
    - Walkthrough video is recorded
  </acceptance_criteria>
  <done>Dev server booted, subagent execution completed, and visual walkthrough verified.</done>
</task>

<task type="auto">
  <name>Task 3: Execute database E2E cleanup script</name>
  <files>web/cleanup-e2e.cjs</files>
  <read_first>web/cleanup-e2e.cjs</read_first>
  <action>
    Run the E2E database cleanup script:
    `node web/cleanup-e2e.cjs`
    Ensure it prints successful deletion logs.
  </action>
  <verify>
    Verify command exits with code 0.
  </verify>
  <acceptance_criteria>
    - Deletes E2E test records from Supabase tables successfully
  </acceptance_criteria>
  <done>E2E database test user is cleaned up, preserving database cleanliness.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` completes successfully with exit code 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Full E2E user flows and database interactions verified successfully
</success_criteria>

<output>
After completion, create `.planning/phases/05-full-system-integration-verification/05-01-SUMMARY.md`
</output>
