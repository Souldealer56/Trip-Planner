---
phase: 03-trips-list-details-ui-views
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/utils/format.js
  - web/src/styles/global.css
  - web/src/views/TripsList.jsx
autonomous: true
requirements:
  - TRIPS-01
user_setup: []

must_haves:
  truths:
    - "Vite compiles successfully and bundles format.js utility"
    - "TripsList imports and triggers useTrips data hook correctly"
    - "Pulsing skeleton loader blocks are displayed while loading is true"
    - "Suitcase empty state card displays when trips array is empty"
  artifacts:
    - "web/src/utils/format.js"
    - "web/src/views/TripsList.jsx"
  key_links:
    - "web/src/views/TripsList.jsx imports useTrips from web/src/hooks/useTrips.js"
    - "web/src/views/TripsList.jsx imports formatDateRange from web/src/utils/format.js"
---

<objective>
Implement the date/currency formatter utility, define CSS layout animation rules for card hover states and loading skeletons, and implement the responsive, dynamic Trips List view.

Purpose: Delivers a premium, styled grid view of all existing trip plans dynamically loaded from Supabase.
Output: Formatter utility script, CSS layout updates, and the TripsList page view.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trips-list-details-ui-views/03-CONTEXT.md
@.planning/phases/03-trips-list-details-ui-views/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement formatters and add pulse skeletons and hover animations to CSS</name>
  <files>web/src/utils/format.js, web/src/styles/global.css</files>
  <read_first>.planning/phases/03-trips-list-details-ui-views/03-CONTEXT.md</read_first>
  <action>
    Create `web/src/utils/format.js`. Implement and export `formatDateRange(startStr, endStr)` (e.g. `Jul 12 – Jul 18, 2026`) and `formatCurrency(amount, currencyCode)` (e.g. converting `USD` to `$`) per D-03.
    Modify `web/src/styles/global.css` to append standard responsive styling classes, grid systems (`.grid-layout`), animated pulsing loaders (`.skeleton`, `@keyframes pulse`), hover glowing borders (`.trip-card:hover`), and header action designs per D-01 and D-04.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/src/utils/format.js` exists and exports formatDateRange and formatCurrency
    - `web/src/styles/global.css` contains `@keyframes pulse` and `.skeleton`
    - `web/src/styles/global.css` contains hover transition selectors
  </acceptance_criteria>
  <done>Date/currency formatting utility is created and global stylesheets are loaded with modern skeletons and hover transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Build the responsive TripsList view page</name>
  <files>web/src/views/TripsList.jsx</files>
  <read_first>web/src/hooks/useTrips.js, web/src/utils/format.js, .planning/phases/03-trips-list-details-ui-views/03-CONTEXT.md</read_first>
  <action>
    Modify `web/src/views/TripsList.jsx` per D-01, D-02, and D-04.
    Import `useTrips` from `../hooks/useTrips`.
    Import `formatDateRange` from `../utils/format`.
    Import `Link` from `react-router-dom`.
    Inside the component:
    1. Trigger `useTrips()` to fetch data.
    2. If `loading` is true, render a beautiful, pulsing card layout using `.skeleton` blocks.
    3. If `error` is non-empty, render a custom glassmorphism error notification card with a "Retry" button linked to the hook's `refresh` callback.
    4. If trips are loaded and empty, render a centered "No Trips Found" card containing a suitcase icon / SVG.
    5. If trips exist, render them inside a grid where each card is wrapped in a `<Link to={"/trips/" + trip.id}>` that applies `.trip-card` hover transformations.
    Provide a "Refresh" header button.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/src/views/TripsList.jsx` imports `Link` from `react-router-dom`
    - View renders loading skeletons, error stubs with refresh button, and trip card grids wrapped in Links
  </acceptance_criteria>
  <done>TripsList view is fully implemented with dynamic loading, error boundaries, empty states, and card navigation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` completes successfully with exit code 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-trips-list-details-ui-views/03-01-SUMMARY.md`
</output>
