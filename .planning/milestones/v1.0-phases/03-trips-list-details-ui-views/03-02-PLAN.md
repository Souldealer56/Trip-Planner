---
phase: 03-trips-list-details-ui-views
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/src/views/TripDetails.jsx
autonomous: true
requirements:
  - TRIPS-02
  - DETAIL-01
  - DETAIL-02
user_setup: []

must_haves:
  truths:
    - "Vite compiles successfully and bundles updated TripDetails component"
    - "TripDetails imports and resolves useParams trip ID correctly"
    - "Trip metadata and base currency are rendered cleanly"
    - "Participant RSVP card roster is dynamically listed with status tags"
  artifacts:
    - "web/src/views/TripDetails.jsx"
  key_links:
    - "web/src/views/TripDetails.jsx imports useTripDetails and useRsvpRoster hooks"
    - "web/src/views/TripDetails.jsx imports formatCurrency from web/src/utils/format.js"
---

<objective>
Implement the Trip Details view page to load metadata and RSVP rosters, displaying detailed trip plans and participants with loading feedback and error handling.

Purpose: Delivers a dedicated view showing comprehensive trip planning info and RSVP statuses.
Output: TripDetails page view.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trips-list-details-ui-views/03-CONTEXT.md
@.planning/phases/03-trips-list-details-ui-views/03-RESEARCH.md
@.planning/phases/03-trips-list-details-ui-views/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the detailed TripDetails page view</name>
  <files>web/src/views/TripDetails.jsx</files>
  <read_first>web/src/hooks/useTripDetails.js, web/src/hooks/useRsvpRoster.js, web/src/utils/format.js, .planning/phases/03-trips-list-details-ui-views/03-CONTEXT.md</read_first>
  <action>
    Modify `web/src/views/TripDetails.jsx` per D-01, D-03, and D-04.
    Import `useParams`, `Link` from `react-router-dom`.
    Import `useTripDetails` from `../hooks/useTripDetails`.
    Import `useRsvpRoster` from `../hooks/useRsvpRoster`.
    Import `formatDateRange` and `formatCurrency` from `../utils/format`.
    Inside the component:
    1. Parse `id` using `useParams()`.
    2. Load trip metadata via `useTripDetails(id)`.
    3. Load RSVP roster via `useRsvpRoster(id)`.
    4. If either is loading, render pulsing loaders (e.g. skeleton elements).
    5. If query fails, render a beautiful glassmorphism error notification card with a "Retry" button linked to both hooks' `refresh` functions.
    6. Render layout:
       - Header: Back link to `/trips`, manual "Refresh" button triggering both hook refreshes.
       - Trip Info Card: title, destination, dates (using helper), and base currency.
       - RSVP Roster Card: display a list of all participants, showing their names, usernames (if present), and status badges (e.g. Committed, Interested) styled with premium borders and typography.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/src/views/TripDetails.jsx` imports dynamic hooks and utilities
    - Page renders header back actions, trip details metadata card, and roster list elements
  </acceptance_criteria>
  <done>TripDetails view is fully implemented and compiles cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` completes successfully with exit code 0
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-trips-list-details-ui-views/03-02-SUMMARY.md`
</output>
