---
phase: 01-project-setup-database-wiring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/src/App.jsx
  - web/src/main.jsx
  - web/src/styles/variables.css
  - web/src/styles/global.css
autonomous: true
requirements:
  - SETUP-01
user_setup: []

must_haves:
  truths:
    - "Vite React app starts and displays without crashing on the dev port"
    - "Navigating to /trips renders the TripsList view stub"
    - "Navigating to /trips/any-id renders the TripDetails view stub"
  artifacts:
    - "web/package.json"
    - "web/src/App.jsx"
    - "web/src/styles/variables.css"
    - "web/src/styles/global.css"
  key_links:
    - "web/src/main.jsx imports web/src/App.jsx"
    - "web/src/main.jsx imports web/src/styles/global.css"
    - "web/src/styles/global.css imports web/src/styles/variables.css"
---

<objective>
Initialize the React + Vite + Vanilla CSS project in the web/ subdirectory, clean up default template boilerplate, set up a centralized styling tokens system, and configure React Router navigation with stub views.

Purpose: Establishes a clean, structured repository setup that separates the web frontend from Python bot code and configures routing.
Output: Initialized React application, routing framework, and centralized styling files.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-database-wiring/01-CONTEXT.md
@.planning/phases/01-project-setup-database-wiring/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Vite project initialization in the web/ directory</name>
  <files>web/package.json, web/vite.config.js, web/index.html</files>
  <read_first>.planning/phases/01-project-setup-database-wiring/01-CONTEXT.md</read_first>
  <action>
    First, run the command `npx -y create-vite@latest --help` to check options.
    Then, initialize the React project in the `web` folder.
    Use: `npx -y create-vite@latest web --template react` (or run inside the folder if preferred, per D-01).
    After the folder is initialized, install base npm dependencies (`npm install` in the web directory).
  </action>
  <verify>
    Run: `test -f web/package.json`
  </verify>
  <acceptance_criteria>
    - `web/package.json` exists
    - `web/vite.config.js` exists
    - `web/package.json` contains dependencies for `react` and `react-dom`
  </acceptance_criteria>
  <done>Vite React application scaffold is generated inside the web/ directory.</done>
</task>

<task type="auto">
  <name>Task 2: Configure React Router and create routing stub views</name>
  <files>web/package.json, web/src/App.jsx, web/src/views/TripsList.jsx, web/src/views/TripDetails.jsx</files>
  <read_first>web/package.json, .planning/phases/01-project-setup-database-wiring/01-CONTEXT.md</read_first>
  <action>
    Install `react-router-dom` in the `web/` directory.
    Create stub views in `web/src/views/TripsList.jsx` and `web/src/views/TripDetails.jsx` that export basic React components rendering "Trips List View Stub" and "Trip Details View Stub".
    Modify `web/src/App.jsx` to implement routing using `react-router-dom` per D-03.
    Set up the following paths:
    - `/` redirects to `/trips`
    - `/trips` renders `<TripsList />`
    - `/trips/:id` renders `<TripDetails />`
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/package.json` dependencies contain `react-router-dom`
    - `web/src/App.jsx` imports `react-router-dom` components (`BrowserRouter`, `Routes`, `Route`)
    - `web/src/views/TripsList.jsx` exists and exports a TripsList component
    - `web/src/views/TripDetails.jsx` exists and exports a TripDetails component
  </acceptance_criteria>
  <done>react-router-dom is installed and configured in App.jsx with TripsList and TripDetails routes.</done>
</task>

<task type="auto">
  <name>Task 3: Clean up boilerplate and establish centralized styling tokens</name>
  <files>web/src/styles/variables.css, web/src/styles/global.css, web/src/main.jsx, web/src/index.css, web/src/App.css</files>
  <read_first>web/src/main.jsx, .planning/phases/01-project-setup-database-wiring/01-CONTEXT.md</read_first>
  <action>
    Delete default template stylesheets `web/src/App.css` and `web/src/index.css`.
    Create `web/src/styles/variables.css` containing CSS Custom Properties for theme colors, spacing, borders, shadows, and smooth transition tokens per D-04.
    Create `web/src/styles/global.css` importing `variables.css` (`@import "./variables.css";`), setting global resets, base layout rules, and loading custom fonts if needed.
    Update `web/src/main.jsx` to import `src/styles/global.css` instead of `index.css`.
    Update `web/src/App.jsx` to remove imports of `App.css`.
  </action>
  <verify>
    Run: `test -f web/src/styles/variables.css`
  </verify>
  <acceptance_criteria>
    - `web/src/styles/variables.css` exists and defines CSS variables under `:root`
    - `web/src/styles/global.css` exists and has `@import "./variables.css";`
    - `web/src/main.jsx` does NOT import `index.css` or `App.css`
  </acceptance_criteria>
  <done>Default CSS files are removed, and centralized variables.css/global.css files are integrated into the React app entry point.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` passes successfully with exit code 0
- [ ] Verify that React Router is imported and routing stub views exist
- [ ] Verify CSS variables stylesheet is created and imported in the entrypoint
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend project is fully scaffolded and builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-database-wiring/01-01-SUMMARY.md`
</output>
