---
phase: 01-project-setup-database-wiring
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - web/package.json
  - web/vite.config.js
  - web/src/services/supabase.js
  - web/verify-db.cjs
autonomous: true
requirements:
  - SETUP-02
user_setup: []

must_haves:
  truths:
    - "Vite compiles successfully and bundles the Supabase client library"
    - "Supabase client correctly loads URL and Key from the root .env file"
    - "Database connectivity test successfully queries the trips table"
  artifacts:
    - "web/src/services/supabase.js"
    - "web/verify-db.cjs"
  key_links:
    - "web/vite.config.js specifies envDir: '../' and envPrefix: ['SUPABASE_', 'VITE_']"
    - "web/src/services/supabase.js imports @supabase/supabase-js and initializes Client"
---

<objective>
Configure the Supabase client integration to fetch credentials from the root .env file, write the Supabase client service, and establish a database connectivity check script.

Purpose: Establishes a verified data link between the web application and the existing Supabase PostgreSQL backend.
Output: Supabase client service file, Vite environment variable configuration, and connection check script.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-database-wiring/01-CONTEXT.md
@.planning/phases/01-project-setup-database-wiring/01-RESEARCH.md
@.planning/phases/01-project-setup-database-wiring/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Supabase JS client and configure Vite environment directories</name>
  <files>web/package.json, web/vite.config.js</files>
  <read_first>web/vite.config.js, .planning/phases/01-project-setup-database-wiring/01-CONTEXT.md</read_first>
  <action>
    Install `@supabase/supabase-js` inside the `web/` directory.
    Modify `web/vite.config.js` to add `envDir: '../'` and `envPrefix: ['SUPABASE_', 'VITE_']` options under the defineConfig block per D-02.
    This ensures that the Vite server reads the existing `.env` file in the parent folder, exposing the `SUPABASE_URL` and `SUPABASE_KEY` variables to the frontend.
  </action>
  <verify>
    Run: `npm run build --prefix web`
  </verify>
  <acceptance_criteria>
    - `web/package.json` dependencies contain `@supabase/supabase-js`
    - `web/vite.config.js` contains `envDir: '../'`
    - `web/vite.config.js` contains `envPrefix:` containing `'SUPABASE_'`
  </acceptance_criteria>
  <done>@supabase/supabase-js is installed and Vite is configured to load environment variables from the root .env directory.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Supabase client service</name>
  <files>web/src/services/supabase.js</files>
  <read_first>web/vite.config.js, .planning/phases/01-project-setup-database-wiring/01-CONTEXT.md</read_first>
  <action>
    Create `web/src/services/supabase.js` per D-02.
    Import `createClient` from `@supabase/supabase-js`.
    Retrieve variables `import.meta.env.SUPABASE_URL` and `import.meta.env.SUPABASE_KEY`.
    Add runtime assertions ensuring both values are present, throw an informative error if they are missing.
    Instantiate and export the `supabase` client.
  </action>
  <verify>
    Run: `test -f web/src/services/supabase.js`
  </verify>
  <acceptance_criteria>
    - `web/src/services/supabase.js` exists
    - File contains `import { createClient }`
    - File exports a `supabase` client instantiated with `import.meta.env.SUPABASE_URL` and `import.meta.env.SUPABASE_KEY`
  </acceptance_criteria>
  <done>Supabase client is configured and exported from services/supabase.js.</done>
</task>

<task type="auto">
  <name>Task 3: Implement and run database connectivity check script</name>
  <files>web/verify-db.cjs</files>
  <read_first>web/src/services/supabase.js, .env, .planning/phases/01-project-setup-database-wiring/01-CONTEXT.md</read_first>
  <action>
    Create `web/verify-db.cjs`.
    Since this script runs in the Node CLI, it must load environment variables from the parent `.env` file.
    Write logic to:
    1. Read and parse the `../.env` file (using Node `fs` and string matching or a quick helper) to extract `SUPABASE_URL` and `SUPABASE_KEY`.
    2. Import `@supabase/supabase-js` (`require('@supabase/supabase-js')`).
    3. Initialize a Supabase client with the extracted URL and Key.
    4. Perform a test query: `supabase.from('trips').select('id').limit(1)`.
    5. If query succeeds, log "Database Connection Successful!" and exit 0. If query fails or environment variables are missing, log the error and exit 1.
  </action>
  <verify>
    Run: `node web/verify-db.cjs`
  </verify>
  <acceptance_criteria>
    - `web/verify-db.cjs` exists
    - Script successfully executes, connects to Supabase, and logs success
  </acceptance_criteria>
  <done>verify-db.cjs script is created and successfully verifies database query execution.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix web` passes successfully with exit code 0
- [ ] `node web/verify-db.cjs` executes and exits with code 0 (logs successful query to 'trips')
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Supabase connection is successfully verified against the live database
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-database-wiring/01-02-SUMMARY.md`
</output>
