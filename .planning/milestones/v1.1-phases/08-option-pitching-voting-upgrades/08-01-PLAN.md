---
phase: "08-option-pitching-voting-upgrades"
plan: "08-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "main.py"
  - "tests/test_pitch_voting.py"
autonomous: true
must_haves:
  truths:
    - "D-01: Schema migration ALTER TABLE poll_options ADD COLUMN description TEXT; is documented for user execution."
    - "D-02: Description prompt step is integrated into the option pitching PM wizard after option name selection."
    - "D-03: Pitching wizard insert queries save description field to Supabase poll_options table."
    - "D-04: Option hyperlinks format option names directly as HTML inline links in companion voting messages."
    - "D-05: Group command /polls queries active polls and dynamically resolves real-time voter counts from Telegram."
  artifacts:
    - "New PM wizard states and handlers for option descriptions in main.py"
    - "New group command /polls in main.py"
    - "tests/test_pitch_voting.py containing unit tests for description prompts, insertions, HTML hyperlinks, and /polls command"
---

# Plan: Phase 8 - Option Pitching & Voting Upgrades

## User Review Required

> [!IMPORTANT]
> This phase requires executing a database schema migration to add the `description` column to the `poll_options` table. Please run the following SQL command in your Supabase dashboard SQL Editor before starting the execution:
> ```sql
> ALTER TABLE poll_options ADD COLUMN description TEXT;
> ```

## Open Questions

None.

## Threat Model

<threat_model>
### Trust Boundaries
1. **Telegram Gateway Boundary:** Receives text inputs from users.
2. **Bot Process Boundary:** Processes text and generates HTML strings.

### STRIDE Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering / XSS | HTML Message Generation | Medium | MITIGATED | Escape user-controlled descriptions and option names (using python's `html.escape` / `_escape` helper) before formatting them as HTML inline links or list text blocks. |
</threat_model>

## Proposed Changes

### TripSync Bot Server

#### [NEW] [test_pitch_voting.py](file:///c:/Users/alex_/Documents/Trip%20Planner/tests/test_pitch_voting.py)
Create unit tests validating option description states, database insertion queries, HTML hyperlink formatting, and the `/polls` tallies command.

#### [MODIFY] [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
- Define new state constant `ADD_OPT_DESC` for conversation handler.
- Update `pm_wizard_handler` routing map to include the description state.
- Implement `wiz_addopt_desc` handler prompting for optional description with skip button.
- Refactor `wiz_addopt_name` to return `ADD_OPT_DESC` and `wiz_addopt_desc` to return `ADD_OPT_DATES`.
- Update `wiz_addopt_price` to insert `description` into `poll_options` table.
- Refactor `finalize_vote` companion message construction to display inline HTML hyperlinks directly on option names and print descriptions if present.
- Implement `/polls` command query and dynamic Telegram poll tallies.

---

## Plan

### Wave 0: Test Setup
1. **Create unit test file**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - Create [test_pitch_voting.py](file:///c:/Users/alex_/Documents/Trip%20Planner/tests/test_pitch_voting.py) containing test cases for pitching description prompts, database inserts, HTML hyperlink formatting, and the `/polls` command.
   - `<acceptance_criteria>`
     - Test file exists and is parseable.

### Wave 1: Pitching Wizard Updates
2. **Update wizard states and add description prompt**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-02: Add state constant `ADD_OPT_DESC` and update `pm_wizard_handler` configuration block.
     - Implement `wiz_addopt_desc(update, context)` that prompts for description with a `Skip description` button, saving to `context.user_data["wiz_desc"]`.
     - Update `wiz_addopt_name` to transition to `ADD_OPT_DESC`.
     - Update `wiz_addopt_desc` to transition to `ADD_OPT_DATES`.
   - `<acceptance_criteria>`
     - Wizard transitions successfully through the new description state.
     - `pytest tests/test_pitch_voting.py -k test_pitch_wizard_description` passes.

3. **Update database insertion with description**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-03: Update `wiz_addopt_price` query to select and insert `"description": context.user_data.get("wiz_desc")` into the `poll_options` table.
   - `<acceptance_criteria>`
     - Pitch option insertion includes the description field.
     - `pytest tests/test_pitch_voting.py -k test_option_db_insert` passes.

### Wave 2: Hyperlinks and `/polls` Command
4. **Refactor companion message hyperlink formatting**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-04: Update `finalize_vote` helper to render the option name as an HTML anchor tag if `link` is present (e.g., `<b>{i}. <a href="{link}">{name}</a></b>`).
     - Display the optional description on a separate line prefixed by a note emoji if `description` is present, properly escaped (e.g. `\n   📝 <i>{_escape(desc)}</i>`).
   - `<acceptance_criteria>`
     - Companion messages display clean inline links and descriptions.
     - `pytest tests/test_pitch_voting.py -k test_companion_message_formatting` passes.

5. **Implement `/polls` vote tallies command**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-05: Implement `polls(update, context)` handler.
     - Fetch active polls from `active_polls` table for the current trip.
     - Query each poll dynamically from Telegram using `context.bot.get_poll(telegram_poll_id)`.
     - Correlate vote results back to their full names using `poll_options_json` and format in a structured overview listing the number of votes and percentages.
     - Register `polls` command handler in `main()`.
   - `<acceptance_criteria>`
     - `/polls` command lists active polls and dynamic tallies.
     - `pytest tests/test_pitch_voting.py -k test_polls_command_tallies` passes.

---

## Verification Plan

### Automated Tests
- Run Phase 8 tests:
  `pytest tests/test_pitch_voting.py`
- Run full project test suite:
  `pytest`

### Manual Verification
- Pitch an option with a link and a description, verify the wizard flows and skips correctly.
- Call `/vote` and verify that the companion message formats the name as an inline hyperlink and prints the description.
- Vote in the group chat poll and execute `/polls` to verify the vote counts match.
