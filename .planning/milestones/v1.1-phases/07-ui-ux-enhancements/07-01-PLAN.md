---
phase: "07-ui-ux-enhancements"
plan: "07-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "main.py"
  - "tests/test_ui_ux.py"
autonomous: true
must_haves:
  truths:
    - "D-01: Centralized emoji dictionary maps all category, module, and alert keys."
    - "D-02: Emojis mapped as: Accommodation 🏠, Flights ✈️, Activities 🎟️, Food 🍴, Transport 🚗, Other ✨, Roster 📋, Budget 📊, Ledger 🧾, Itinerary 📅."
    - "D-03: Lists in roster, budget, ledger, and settle commands use standard bullet points (•) and bold subject texts."
    - "D-04: Itinerary schedules use category emoji prefixing and bold category titles."
    - "D-05: Transaction notifications, warnings, and error reports use a standardized bold-header layout."
  artifacts:
    - "_UX_EMOJIS registry dictionary in main.py"
    - "tests/test_ui_ux.py containing unit tests validating formatting output and emoji resolutions"
---

# Plan: Phase 7 - UI & UX Enhancements

## User Review Required

> [!NOTE]
> All messaging templates will use standard HTML tags for parsing. Standard HTML characters (`<`, `>`, `&`) in user-provided fields (like trip titles, option names, descriptions, or participant names) will be escaped to prevent parsing failures or display anomalies.

## Open Questions

None.

## Threat Model

<threat_model>
### Trust Boundaries
1. **Telegram Gateway Boundary:** Receives text inputs from users.
2. **Bot Process Boundary:** Processes text and generates HTML strings.

### STRIDE Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering / XSS | HTML Message Generation | Medium | MITIGATED | Escape user-controlled input text (using python's `html.escape`) before injecting it into HTML-formatted strings to prevent formatting breakages or injection blocks. |
</threat_model>

## Proposed Changes

### TripSync Bot Server

#### [NEW] [test_ui_ux.py](file:///c:/Users/alex_/Documents/Trip%20Planner/tests/test_ui_ux.py)
Create unit tests validating roster, ledger, budget, itinerary formatting, emoji lookups, and alert structures.

#### [MODIFY] [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
Refactor formatting structures, centralize emoji map dictionary, escape HTML inputs, and standardize response outputs.

---

## Plan

### Wave 0: Test Setup
1. **Create unit test file**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - Create [test_ui_ux.py](file:///c:/Users/alex_/Documents/Trip%20Planner/tests/test_ui_ux.py) containing test cases for emoji lookup, roster output formatting, ledger/budget formatting, itinerary layout, and alert text formats.
   - `<acceptance_criteria>`
     - Test file exists and is parseable.

### Wave 1: Centralization & Roster Update
2. **Implement centralized emoji map registry**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-01/D-02: Implement global dictionary `_UX_EMOJIS` containing standardized category, command, and status keys.
     - Add helper function `_escape(text: str) -> str` using `html.escape` to safely sanitize strings.
   - `<acceptance_criteria>`
     - `_UX_EMOJIS` registered.
     - `pytest tests/test_ui_ux.py -k test_emoji_consistency` passes.

3. **Standardize roster command formatting**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-03: Update `roster` command handler in `main.py` to use `•` (bullet points) and bold names (e.g. `• <b>Alex</b>`), pulling emojis from `_UX_EMOJIS`.
   - `<acceptance_criteria>`
     - `/roster` output matches standardized formatting.
     - `pytest tests/test_ui_ux.py -k test_roster_formatting` passes.

### Wave 2: Reports & Alert Cohesion
4. **Standardize ledger and budget command formatting**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-03: Update `ledger`, `settle`, and `budget` command handlers to use `•` (bullet points) and bold names/subjects uniformly, resolving emojis from the central dictionary.
   - `<acceptance_criteria>`
     - `/ledger`, `/settle`, and `/budget` layouts aligned.
     - `pytest tests/test_ui_ux.py -k test_ledger_budget_formatting` passes.

5. **Standardize itinerary command formatting**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-04: Update `itinerary` handler to use block layouts prefixed with category emojis and bold category titles (e.g., `🏠 <b>Accommodation:</b> Hilton`).
     - Remove the local `CATEGORY_EMOJI` dictionary.
   - `<acceptance_criteria>`
     - `/itinerary` outputs updated.
     - `pytest tests/test_ui_ux.py -k test_itinerary_formatting` passes.

6. **Align confirmations, alerts, and warning layouts**
   - `<read_first>`
     - [main.py](file:///c:/Users/alex_/Documents/Trip%20Planner/main.py)
   - `<action>`
     - D-05: Standardize transaction alerts, database warnings, and group announcements to use the uniform bold-header formatting structure (e.g. `✅ <b>[Header]</b>\n[Details]`).
   - `<acceptance_criteria>`
     - Bot success messages, warning alerts, and error dialogues restructured.
     - `pytest tests/test_ui_ux.py -k test_alert_formatting` passes.

---

## Verification Plan

### Automated Tests
- Run quick tests on the new UI/UX module:
  `pytest tests/test_ui_ux.py`
- Run the full suite to verify no regressions:
  `pytest`

### Manual Verification
- Deploy and start the bot.
- Invoke `/roster`, `/budget`, `/ledger`, and `/itinerary` and verify the messaging layouts.
- Pitch an option and log an expense and verify the announcement alerts.
