---
phase: 13-webapp-debts-settlement-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/utils/currency.js
  - web/src/views/TripDetails.jsx
  - web/src/styles/global.css
  - web/verify-settlement.cjs
autonomous: false
requirements:
  - LEDG-03
must_haves:
  truths:
    - "D-01: The webapp fetches live exchange rates from open.er-api.com on load and caches them in memory."
    - "D-02: Converted values use USD as the baseline intermediary to convert between non-USD currencies."
    - "D-03: The settlement solver calculates exact individual balances based on who participated in splits."
    - "D-04: The transaction minimization algorithm matches maximum debtors with maximum creditors greedily."
  artifacts:
    - "web/src/utils/currency.js contains convertCurrency"
    - "web/src/utils/currency.js contains calculateSettlements"
    - "web/verify-settlement.cjs exits with status 0"
  key_links:
    - "TripDetails view renders a Settle Up tab panel displaying settlements."
---

<objective>
Implement live exchange rate fetching, multi-currency conversion utility functions, exact split debt balance calculations, and transaction minimization displays.

Purpose: Let travelers view net balance sheets and the minimum transactions required to settle trip debts.
Output: Custom currency utility file, Node validation test, and a new interactive Settle Up tab inside the details view.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-webapp-debts-settlement-optimization/13-CONTEXT.md
@.planning/phases/13-webapp-debts-settlement-optimization/13-RESEARCH.md
@.planning/phases/13-webapp-debts-settlement-optimization/13-UI-SPEC.md
@web/src/views/TripDetails.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement web/src/utils/currency.js containing currency and solver utilities</name>
  <read_first>
    - .planning/phases/13-webapp-debts-settlement-optimization/13-RESEARCH.md
  </read_first>
  <action>
    Create the utility file web/src/utils/currency.js exporting:
    1. `async function fetchExchangeRates()`: Fetches conversion rates from `https://open.er-api.com/v6/latest/USD` and returns the `rates` dictionary.
    2. `function convertCurrency(amount, fromCurrency, toCurrency, rates)`: Converts standard amounts using USD as intermediary.
    3. `function calculateSettlements(balances)`: Computes debtor/creditor transaction match list from individual balances.
  </action>
  <acceptance_criteria>
    - web/src/utils/currency.js exists and exports: fetchExchangeRates, convertCurrency, calculateSettlements.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build verification script web/verify-settlement.cjs</name>
  <read_first>
    - web/src/utils/currency.js
  </read_first>
  <action>
    Create a Node verification test script web/verify-settlement.cjs that:
    1. Simulates mock rates (USD=1.0, EUR=0.92, GBP=0.78).
    2. Simulates mock balances with exact split net balances.
    3. Runs assertions on convertCurrency and calculateSettlements.
    4. Asserts that settlements outputs correct transaction paths.
  </action>
  <acceptance_criteria>
    - web/verify-settlement.cjs exists.
    - Running `node web/verify-settlement.cjs` exits with code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate Settle Up calculations and tab panel inside TripDetails.jsx</name>
  <read_first>
    - web/src/views/TripDetails.jsx
    - web/src/utils/currency.js
  </read_first>
  <action>
    Modify TripDetails.jsx:
    1. Import currency and solver utilities from utils/currency.
    2. Fetch live exchange rates on component load and store them in state.
    3. Add a tab toggle switch inside the Shared Ledger card: "Ledger" vs. "Settle Up".
    4. Calculate net balances and minimized transactions dynamically:
       - Process all committed roster members and loaded expenses.
       - Apply exact custom split logic.
    5. Render the "Settle Up" panel:
       - Displays Total Spent and Per-Person averages (converted to base currency).
       - Lists participant net balances (credits in emerald green, debits in red).
       - Lists settlement transactions ("Bob owes Alice $15.00").
       - Renders empty state if everyone is settled/even.
  </action>
  <acceptance_criteria>
    - TripDetails.jsx contains the tab toggle switch inside the Ledger section.
    - Settle Up calculations convert multiple currencies relative to trip.base_currency.
    - Settlements are calculated using custom splits.
    - Transaction list and individual balances are displayed cleanly.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add CSS styles for settlements in global.css</name>
  <read_first>
    - web/src/styles/global.css
    - .planning/phases/13-webapp-debts-settlement-optimization/13-UI-SPEC.md
  </read_first>
  <action>
    Add styling classes for debt arrows, visual layout tabs, and credit/debit badges to the bottom of web/src/styles/global.css.
  </action>
  <acceptance_criteria>
    - CSS styling classes added.
    - npm run lint passes successfully.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### Files
- `web/src/utils/currency.js` — Exchange rate client fetcher and debt settlement solvers
- `web/verify-settlement.cjs` — Solver calculations validation test script

### Functions
- `fetchExchangeRates()`
- `convertCurrency(amount, fromCurrency, toCurrency, rates)`
- `calculateSettlements(balances)`
