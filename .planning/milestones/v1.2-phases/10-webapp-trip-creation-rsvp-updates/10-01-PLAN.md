---
phase: 10-webapp-trip-creation-rsvp-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/services/trips.js
  - web/src/services/rsvps.js
  - web/src/views/TripsList.jsx
  - web/src/views/TripDetails.jsx
  - web/src/styles/global.css
autonomous: false
requirements:
  - TRIP-03
  - RSVP-04
must_haves:
  truths:
    - "A 'New Trip' modal form can be opened on the Trips List page."
    - "Invalid date ranges (end before start) display validation errors and prevent submission."
    - "Submitting the form inserts a row in trips and redirects to details."
    - "Signed-in users can update their status via a dropdown in the session bar."
  artifacts:
    - "web/src/services/trips.js contains createTrip"
    - "web/src/services/rsvps.js contains updateRsvpStatus"
  key_links:
    - "TripsList redirects to /trips/:id on success."
    - "RSVP dropdown state changes trigger updateRsvpStatus."
---

<objective>
Implement trip creation and RSVP status updates on the React web application.

Purpose: Bridge critical collaborative planning features from the bot to the web view.
Output: Active trip creation forms, RSVP updates services, and visual UI components.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-webapp-trip-creation-rsvp-updates/10-CONTEXT.md
@.planning/phases/10-webapp-trip-creation-rsvp-updates/10-RESEARCH.md
@.planning/phases/10-webapp-trip-creation-rsvp-updates/10-UI-SPEC.md
@web/src/services/trips.js
@web/src/services/rsvps.js
@web/src/views/TripsList.jsx
@web/src/views/TripDetails.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend service functions for trips and RSVPs</name>
  <files>web/src/services/trips.js, web/src/services/rsvps.js</files>
  <action>
    Export async function createTrip(title, destination, startDate, endDate, baseCurrency) in web/src/services/trips.js. The function must insert a row into the trips table and return the inserted single object.
    Export async function updateRsvpStatus(tripId, userId, status) in web/src/services/rsvps.js. The function must update the status of the RSVP matching the trip_id and user_id and return the updated single object.
  </action>
  <verify>
    <automated>node web/verify-services.cjs</automated>
  </verify>
  <done>Backend service methods exist and execute successfully against Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Trip Creation modal in TripsList.jsx and global.css</name>
  <files>web/src/views/TripsList.jsx, web/src/styles/global.css</files>
  <action>
    Add a "New Trip" button in the TripsList header. Clicking it opens a modal containing:
    - Text inputs for Title and Destination
    - Date selectors for Start Date and End Date
    - Dropdown/Select for Base Currency (USD, EUR, GBP, etc., defaulting to USD)
    Add client-side validation ensuring fields are non-empty and End Date is not before Start Date. Display clear error messages.
    Upon successful submission, invoke createTrip and redirect the user using React Router to /trips/:id.
    Define CSS classes for the modal container, overlay background, close button, and invalid input outlines in web/src/styles/global.css.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Modal form displays, validates input fields on client side, inserts record in DB, and redirects on success.</done>
</task>

<task type="auto">
  <name>Task 3: Add RSVP status dropdown in TripDetails.jsx session header bar</name>
  <files>web/src/views/TripDetails.jsx</files>
  <action>
    Add a dropdown selector in the TripDetails user session header bar, visible next to the user's name when signed in.
    The dropdown must display "Committed", "Tentative", "Declined" as selectable options, preselected to the active user's current RSVP status.
    On change, invoke updateRsvpStatus in rsvps.js with the active user ID and new status.
    Refresh the roster list immediately on success to reflect the updated status badge.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Active user can update their RSVP status from the session bar dropdown and see immediate updates in the roster badge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human Verification of Trip Creation & RSVP Selector</name>
  <files>web/src/views/TripsList.jsx, web/src/views/TripDetails.jsx</files>
  <action>
    Manually verify the UI and user flows:
    1. Start the Vite dev server with `npm run dev` in the web folder.
    2. Open the web app URL in the browser.
    3. On the Trips List page, click the "New Trip" button. Verify the modal opens.
    4. Fill in the form. Set an end date earlier than the start date to verify client-side validation prevents submission.
    5. Correct the dates and click "Create Trip". Verify redirect to `/trips/:new-trip-id`.
    6. Join the trip using the "Join Trip" form (since the roster is empty).
    7. Select a different RSVP status from the dropdown. Verify the roster badge updates instantly.
  </action>
  <verify>
    Confirm with the user that the UI works correctly and has been approved.
  </verify>
  <done>Human approves that the trip creation and RSVP dropdown functionality are complete and visually polished.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Supabase API | Web client communicates directly with Supabase via anonymous client credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Spoofing | TripDetails.jsx (RSVP status selector) | medium | mitigate | Validate in frontend that updateRsvpStatus is only invoked for the active logged-in user profile. |
| T-10-02 | Tampering | TripsList.jsx (Date range checks) | medium | mitigate | Enforce client-side date range validations (start_date <= end_date) before sending API inserts. |
| T-10-03 | Information Disclosure | trips database | low | accept | Public trip planning platform without user auth in v1. Linked profile select is sufficient for user workflow. |
</threat_model>

<verification>
Automated verification command: `pytest tests/` and custom integration tests.
</verification>

<success_criteria>
All automated verification commands return successful exit codes. Manual UI walkthrough validates date checks, redirection, and dynamic badges.
</success_criteria>

<output>
Create `.planning/phases/10-webapp-trip-creation-rsvp-updates/10-01-SUMMARY.md` when done
</output>
