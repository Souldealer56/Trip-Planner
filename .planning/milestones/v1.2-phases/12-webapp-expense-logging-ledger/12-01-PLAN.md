---
phase: 12-webapp-expense-logging-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/services/expenses.js
  - web/src/views/TripDetails.jsx
  - web/src/styles/global.css
  - web/verify-expenses-service.cjs
autonomous: false
requirements:
  - LEDG-01
  - LEDG-02
must_haves:
  truths:
    - "D-01: We will extend the Supabase `expenses` schema by adding `currency` (TEXT) and `split_users` (JSONB) columns."
    - "D-02: The `split_users` JSONB column stores user UUIDs participating in the split."
    - "D-03: The 'Add Expense' form modal features a 'Split Equally' toggle."
    - "D-04: Toggling 'Split Equally' OFF reveals checklists of committed roster participants for custom split selections."
  artifacts:
    - "web/src/services/expenses.js contains fetchExpenses"
    - "web/src/services/expenses.js contains logExpense"
    - "web/verify-expenses-service.cjs exits with status 0"
  key_links:
    - "Add Expense modal submission invokes logExpense."
    - "Roster hook useRsvpRoster provides the committed participant list."
---

<objective>
Implement shared expense logging, custom split participant check selectors, and scrollable ledger details tables inside the web application.

Purpose: Let travelers track and log group travel expenses and splitting structures from the web interface.
Output: Dynamic Add Expense modal, responsive ledger list cards, and updated database entries.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-webapp-expense-logging-ledger/12-CONTEXT.md
@.planning/phases/12-webapp-expense-logging-ledger/12-RESEARCH.md
@.planning/phases/12-webapp-expense-logging-ledger/12-UI-SPEC.md
@web/src/views/TripDetails.jsx
@web/src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the verification test script web/verify-expenses-service.cjs</name>
  <read_first>
    - web/verify-services.cjs
    - web/src/services/supabase.js
  </read_first>
  <action>
    Create a new file web/verify-expenses-service.cjs modeled after verify-services.cjs that:
    1. Loads credentials from the parent .env file.
    2. Initializes a Supabase client.
    3. Runs verification assertions for:
       - Creating a test expense inside `expenses` table (trip_id: valid ID, paid_by: valid user ID, amount: 25.50, currency: 'EUR', description: 'Verification Pizza', split_users: [voter_uuids])
       - Fetching expenses for the trip and verifying the payer join (resolving user.first_name)
       - Cleaning up the test expense row
  </action>
  <acceptance_criteria>
    - web/verify-expenses-service.cjs exists.
    - Running `node web/verify-expenses-service.cjs` exits with code 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement web/src/services/expenses.js backend operations</name>
  <read_first>
    - web/src/services/trips.js
    - web/src/services/supabase.js
    - web/verify-expenses-service.cjs
  </read_first>
  <action>
    Create the expenses service file web/src/services/expenses.js and export:
    1. async function fetchExpenses(tripId): Select * from expenses and join user details, ordered by created_at ascending.
    2. async function logExpense(tripId, paidByUserId, amount, currency, description, splitUsers): Insert a row with properties mapping to schema fields and select/return it.
  </action>
  <acceptance_criteria>
    - web/src/services/expenses.js contains exports: fetchExpenses, logExpense.
    - Running `node web/verify-expenses-service.cjs` passes successfully.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build Add Expense form modal and splitting selectors inside TripDetails.jsx</name>
  <read_first>
    - web/src/views/TripDetails.jsx
    - web/src/services/expenses.js
  </read_first>
  <action>
    Integrate an "Add Expense" button in TripDetails.jsx next to the Pitched Options / Roster headers.
    Clicking it opens a modal overlay featuring:
    - Description input field *
    - Amount input field * (number)
    - Currency dropdown (USD, EUR, GBP, CAD, AUD)
    - Paid By selection dropdown (lists all committed roster participants; default to active session user)
    - "Split Equally" toggle switch (default: true).
    - If "Split Equally" is toggled OFF, show checklist of all committed roster participants (retrieved from useRsvpRoster hook). All checked by default.
    Add client-side validations to require description, valid positive amount, and at least one split participant checked.
    Submit form using logExpense service and refresh the ledger.
  </action>
  <acceptance_criteria>
    - Add Expense modal opens on click.
    - Paid By and split participant lists are populated from active committed members.
    - "Split Equally" toggle hides/reveals check fields correctly.
    - Form validation blocks zero/negative values, missing descriptions, or zero checked participants.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Build scrollable ledger table displaying logged expenses inside TripDetails.jsx</name>
  <read_first>
    - web/src/views/TripDetails.jsx
    - web/src/styles/global.css
  </read_first>
  <action>
    Create a "Shared Ledger" section inside TripDetails.jsx (e.g. placed next to or immediately following Roster):
    - Load logged expenses using fetchExpenses service.
    - If empty, render empty state: "No expenses logged yet. Keep track of group travel splits by logging your first expense!"
    - Render a table showing columns: Description, Paid By (first name), Amount, Currency, Date, and Splits (displays list of participant names or "Everyone" if all committed members are checked).
    - On smaller viewports, stack rows cleanly using mobile glassmorphic cards.
    Add styles for ledger tables and checkbox styling inside web/src/styles/global.css.
  </action>
  <acceptance_criteria>
    - Ledger table displays logged description, payer, amount, currency, and date correctly.
    - Displays split participant badges or "Everyone" summary correctly.
    - Render empty state message when no expenses are present.
    - Global styles for ledger table added to global.css.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### Files
- `web/src/services/expenses.js` — Client API service library for ledger expenses
- `web/verify-expenses-service.cjs` — Service integration verification script

### Functions
- `fetchExpenses(tripId)`
- `logExpense(tripId, paidByUserId, amount, currency, description, splitUsers)`
